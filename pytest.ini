[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    unit: fast in-process unit tests
    integration: tests that touch the network or live services
    slow: tests that take more than a second
filterwarnings =
    ignore::DeprecationWarning
